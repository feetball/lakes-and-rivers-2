services:
  web:
    build: .
    image: texas-flood-map:local
    container_name: texas-flood-map
    ports:
      - "3000:3000"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
